# growi-plugin-table-extended

GROWI のページ本文中の Markdown テーブルのヘッダーをクリックして列ソートできるようにする Script プラグインです。

## 機能

- テーブルヘッダー（`th`）をクリックすると該当列で昇順 → 降順 → 元の順 にソートされる
- 数値列・日付列・文字列列をデータ内容から自動判定してソート
- 数値はカンマ区切り（例: `1,234`）に対応
- 日付は `YYYY-MM-DD` / `YYYY/MM/DD` 形式に対応
- テーブル全体に視覚スタイルを付与（角丸・ヘッダー色・偶数行ストライプ・行ホバーハイライト）。色は GROWI のテーマ色（`--bs-primary`）を `color-mix()` で取り込み、テーマに追従する
- ダークモード対応：OS の `prefers-color-scheme: dark` と GROWI の UI トグル（`html[data-bs-theme="dark"]` / Bootstrap 5.3）の両方で配色が切り替わる
- SPA ページ遷移時に新ページのテーブルを自動検知・初期化する
- 閲覧モードのみ動作（編集モードでは無効）
- GROWI 標準のテーブル編集モーダル（`body.modal-open`）が開いている間は矢印・スタイル・クリック動作をすべて停止し、編集 UI を妨げない
- 管理画面（`/admin` 以下）では無効
- 印刷時は矢印を非表示
- `data-no-sort` 属性を付けたテーブルは対象外にできる
- プラグイン無効化時にすべてのリスナー・DOM 変更を完全クリーンアップ

## インストール

GROWI の管理画面 `/admin/plugins` でこのリポジトリの URL を入力してインストールしてください。

インストール後、プラグインを有効化するとテーブルのヘッダーがクリック可能になります。

> **注意**: コード更新を反映する際は、管理画面でプラグインを**削除 → 再インストール**してください。有効/無効のトグルのみでは zip が取り直されません。

## 使い方

### 基本的なソート

Markdown でテーブルを書くだけで自動的にソート機能が有効になります。

```markdown
| 名前   | 年齢 | 入社日     |
|--------|------|------------|
| 田中   | 30   | 2020-04-01 |
| 鈴木   | 25   | 2022-08-15 |
| 佐藤   | 35   | 2018-01-10 |
```

### ソートを無効にしたいテーブル

HTML の `data-no-sort` 属性をテーブルに付与するとソート対象外にできます（Markdown では直接指定不可のため、HTML ブロックとして記述してください）。

## 開発・ビルド

### セットアップ

```bash
pnpm install
pnpm approve-builds --all   # 初回のみ（pnpm 11+ の場合）
pnpm install                 # esbuild の postinstall を実行するため再度
```

### ビルド

```bash
pnpm build
```

`dist/manifest.json` と `dist/assets/` が生成されます。`dist/` はコミット必須です（GROWI はビルドを実行しないため）。

### 動作確認チェックリスト

- [ ] `pnpm build` が成功し `dist/manifest.json` が出力される
- [ ] GROWI で削除 → 再インストール後、DevTools Network で `client-entry-*.js` が 200 で取得される
- [ ] `th` クリックで ⇅ → ▲（昇順）→ ▼（降順）→ ⇅（復元）に変化する
- [ ] 数値列（カンマ区切り含む）が数値順でソートされる
- [ ] 日付列が時系列でソートされる
- [ ] 別 `th` クリックで前の列がリセットされる
- [ ] SPA 遷移後のテーブルも自動でソート可能になる
- [ ] 編集モード中はソートが無効になる
- [ ] 編集モード終了後に再度ソート可能になる
- [ ] 管理画面（`/admin`）ではソートが無効になる
- [ ] 印刷プレビューで矢印が非表示になる
- [ ] プラグイン無効化で全テーブルが元の状態に完全復元される
- [ ] ダークモード切替（OS 設定 / GROWI UI トグル）でテーブルの配色が追従する
- [ ] テーブル編集モーダルを開いている間は矢印・スタイルが消え、編集ボタンが正常に押せる
- [ ] テーブル編集モーダルを閉じた後、自動でソート可能・スタイル付き状態に戻る

## ライセンス

MIT
